{
  "name": "tiktok-downloader",
  "version": "1.0.0",
  "description": "TikTok Video Downloader App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "*",
    "cors": "*",
    "ruhend-scraper": "*"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
